# Dépendances Python pour AudiobookForge
# Installation : pip install -r requirements.txt

# Extraction de texte
ebooklib>=0.18
beautifulsoup4>=4.12
pymupdf>=1.23
python-docx>=1.1

# Synthèse vocale (mlx-speech) — Apple Silicon uniquement
mlx-speech>=0.4
numpy>=2.0
soundfile>=0.13

# API HTTP (pour le backend Docker)
httpx>=0.27

